'use strict';

var exp = {
    database: {
        host: "localhost",
        port: 3306,
        name: "systemet",
        user: "systemet",
        password: "systemet"
    }
};

module.exports = exp;
